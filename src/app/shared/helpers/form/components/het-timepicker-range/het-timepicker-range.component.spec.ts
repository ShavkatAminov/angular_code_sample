import { HetTimepickerRangeComponent } from './het-timepicker-range.component'

describe('HetTimepickerRangeComponent', () => {
  it('should mount', () => {
    //cy.mount(HetTimepickerRangeComponent)
  })
})
